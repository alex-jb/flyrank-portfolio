# Image Curation — Week 3

**Alex Ji · FlyRank AI-Fluency · "Curate Your Images"** · full layout: [`curate-images.pdf`](./curate-images.pdf)

The assignment isn't "make images" — it's *choose* images that serve the proof and look like a set. Kit: IBM Plex + slate/teal.

## 1. Image map (matched to my sitemap)

| Section | Image it needs | Type | Status |
|---|---|---|---|
| Hero | faint agent-node lattice texture | **AI / designed** | keeper → `hero-keeper.png` |
| Work — Orallexa | 5-voice debate output, or 54★ repo + test badge | **REAL capture** | Alex (shot list §4) |
| Work — Solo Founder OS | PyPI page / `pip install` + green tests | **REAL capture** | Alex |
| Work — Shadow | `/api/deliberate` JSON + hash-chain audit, or MCP in Cursor | **REAL capture** | Alex |
| Section markers | 4 line icons (agents / stack / audit / CTA) | **AI / designed** | keeper → `icons.png` |
| About | photo of me | **REAL photo** | Alex's headshot |
| Contact | none — a CTA button | — | — |

## 2. Connective-tissue set (AI/designed, one style)

`hero-keeper.png` + `icons.png` — same slate + teal, same restraint. **A set, not a pile.** The hero recedes so the work is the loudest thing on the page.

## 3. Rejected — the discernment call (graded)

**Rejected** `hero-rejected.png` (a colourful gradient-mesh "AI-startup" texture). **Why:** it's the exact generic AI-slop aesthetic every vibe-coded landing page ships — *louder* than my work, it breaks my own "calm frame" rule, and its rainbow palette blows up the slate+teal kit. It would read as **bought, not built.** **Kept** the faint agent-node lattice instead: it reinforces the multi-agent story quietly, stays in-palette, and recedes behind the claim and the real screenshots.

## 4. Where I chose REAL over AI (+ shot list)

Every **work** image is a real screenshot, never generated. My whole pitch is *"you can audit my work"* — a fake dashboard would undercut the one thing I'm selling. AI is used only for texture and icons; the proof is always real.

Real captures to take (in a logged-in browser — clean, cropped, legible):
1. **Orallexa** — live demo `orallexa-ui.vercel.app` (5-voice debate), or the GitHub header with **54★ + 1,300+ tests**.
2. **Solo Founder OS** — PyPI page or terminal `pip install` + green test run.
3. **Shadow** — a `/api/deliberate` JSON + hash-chain audit, or MCP tools in Cursor/Claude Desktop.
4. **About** — a real headshot, neutral background.

> Auto-capturing the GitHub/live pages headlessly failed (bot-protection + JS render) — which is the right call anyway: real captures should be *mine*, not a machine's blurry grab.
